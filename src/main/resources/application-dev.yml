spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/vshpi_dev}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:pass}

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

logging:
  level:
    org.hibernate.SQL: DEBUG