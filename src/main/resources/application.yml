server:
  port: 8080

spring:
  profiles:
    active: dev

app:
  name: "Task Manager"
  version: "1.0.0"

  session:
    attributes:
      favorite-tasks: "favoriteTasksIds"

  storage:
    directories:
      task-attachments: "upload"